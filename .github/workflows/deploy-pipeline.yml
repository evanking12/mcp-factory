# .github/workflows/deploy-pipeline.yml
#
# Builds and deploys the pipeline container (Dockerfile) to ACA.
# Triggered whenever api/, src/, or Dockerfile changes on main.
#
# ── Required GitHub Secrets ──────────────────────────────────────────────
#   AZURE_CLIENT_ID       → mcp-factory-identity clientId
#                           f70e3ce7-3790-49de-95b0-2de22fc0adbe
#   AZURE_TENANT_ID       → bfddc1f5-1e88-471c-9b5e-44611ddd3c22
#   AZURE_SUBSCRIPTION_ID → abb10328-e7f1-4d4a-9067-c1967fd70429
#
# ── One-time Azure setup (OIDC federated credential) ─────────────────────
#   az identity federated-credential create \
#     --name github-actions \
#     --identity-name mcp-factory-identity \
#     --resource-group mcp-factory-rg \
#     --issuer https://token.actions.githubusercontent.com \
#     --subject repo:evanking12/mcp-factory:ref:refs/heads/main \
#     --audiences api://AzureADTokenExchange
#
#   Also ensure mcp-factory-identity has AcrPush on mcpfactoryacr and
#   Contributor on mcp-factory-rg.
# ────────────────────────────────────────────────────────────────────────

name: Deploy Pipeline

on:
  push:
    branches: [main]
    paths:
      - 'api/**'
      - 'src/**'
      - 'Dockerfile'
      - 'requirements.txt'
  workflow_dispatch:

permissions:
  id-token: write   # required for OIDC
  contents: read

env:
  ACR:        mcpfactoryacr.azurecr.io
  IMAGE:      mcpfactoryacr.azurecr.io/mcp-factory-pipeline
  APP_NAME:   mcp-factory-pipeline
  RG:         mcp-factory-rg
  KEY_VAULT:  mcp-factory-kv
  BRIDGE_URL: http://20.124.33.45:8090
  BRIDGE_VM_NAME: mcpfactory-runner-vm
  IDENTITY_ID: /subscriptions/abb10328-e7f1-4d4a-9067-c1967fd70429/resourceGroups/mcp-factory-rg/providers/Microsoft.ManagedIdentity/userAssignedIdentities/mcp-factory-identity

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upgrade Azure CLI
        run: az upgrade --yes --all 2>/dev/null || pip install --upgrade azure-cli

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id:       ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id:       ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Login to ACR
        run: az acr login --name mcpfactoryacr

      - name: Build & push image
        run: |
          IMAGE_TAG="${{ env.IMAGE }}:${{ github.sha }}"
          IMAGE_LATEST="${{ env.IMAGE }}:latest"
          docker build -f Dockerfile -t "$IMAGE_TAG" -t "$IMAGE_LATEST" .
          docker push "$IMAGE_TAG"
          docker push "$IMAGE_LATEST"

      - name: Deploy to Container App
        run: |
          az containerapp update \
            --name ${{ env.APP_NAME }} \
            --resource-group ${{ env.RG }} \
            --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }} \
            --image ${{ env.IMAGE }}:${{ github.sha }} \
            --min-replicas 1
          # Ensure ingress always points at the correct port (pipeline listens on 8000)
          az containerapp ingress update \
            --name ${{ env.APP_NAME }} \
            --resource-group ${{ env.RG }} \
            --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }} \
            --target-port 8000

          az containerapp secret set \
            --name ${{ env.APP_NAME }} \
            --resource-group ${{ env.RG }} \
            --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }} \
            --secrets \
              gui-bridge-secret=keyvaultref:https://${{ env.KEY_VAULT }}.vault.azure.net/secrets/gui-bridge-secret,identityref:${{ env.IDENTITY_ID }} \
              pipeline-api-key=keyvaultref:https://${{ env.KEY_VAULT }}.vault.azure.net/secrets/pipeline-api-key,identityref:${{ env.IDENTITY_ID }}

          az containerapp update \
            --name ${{ env.APP_NAME }} \
            --resource-group ${{ env.RG }} \
            --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }} \
            --set-env-vars \
              "GUI_BRIDGE_URL=${{ env.BRIDGE_URL }}" \
              "GUI_BRIDGE_SECRET=secretref:gui-bridge-secret" \
              "PIPELINE_API_KEY=secretref:pipeline-api-key" \
              "AZURE_SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID }}" \
              "AZURE_RESOURCE_GROUP=${{ env.RG }}" \
              "BRIDGE_VM_NAME=${{ env.BRIDGE_VM_NAME }}" \
              "ENABLE_BRIDGE_VM_LIFECYCLE=true" \
              "ENABLE_CORBA_ORB_RUNTIME=true" \
              "BRIDGE_START_TIMEOUT_SECONDS=420" \
              "BRIDGE_IDLE_MINUTES=30"

      - name: Verify deployment
        run: |
          URL=$(az containerapp show \
            --name ${{ env.APP_NAME }} \
            --resource-group ${{ env.RG }} \
            --query properties.configuration.ingress.fqdn -o tsv)
          echo "Pipeline URL: https://$URL"
          # Wait up to 60 s for /health to return 200
          for i in $(seq 1 12); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://$URL/health" || echo 000)
            echo "  attempt $i → $STATUS"
            [ "$STATUS" = "200" ] && echo "✓ Healthy" && exit 0
            sleep 5
          done
          echo "⚠ Health check did not pass in time" && exit 1
