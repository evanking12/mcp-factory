# Sponsor Repo Fixture

This deterministic fixture proves the sponsor requirement that an existing repo
or folder can be profiled. The discovery pass should find invocables across
multiple source files, and the GPT proof selects `repo_echo_sentinel`.

Hint: Contoso customer service tools echo the supplied sentinel so CI can prove
tool execution without relying on external systems.
