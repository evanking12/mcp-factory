# Sponsor Proof Index

This is the static landing page for sponsor proof artifacts.

## Canonical Baseline

- Canonical green run: [24542583216](https://github.com/evanking12/mcp-factory/actions/runs/24542583216)
- Runtime expansion target: the next full green `Sponsor Demo E2E` run on
  `73c8b80` or newer replaces this baseline as canonical after closeout.
- Workflow: [Sponsor Demo E2E](https://github.com/evanking12/mcp-factory/actions/workflows/sponsor-demo-e2e.yml)
- Artifact name: `sponsor-demo-e2e`

## Artifact Map

Inside the `sponsor-demo-e2e` GitHub Actions artifact:

- `final-summary.md`: sponsor-readable pass/fail report.
- `final-summary.json`: machine-readable gate and requirement matrix.
- `sponsor-report.html`: browsable static report.
- `ci_artifacts/demo/gpt-format-matrix/summary.json`: GPT-4o format proof matrix.
  The runtime expansion version includes `runtime_mode_counts`,
  `runtime_backed_cases`, `adapter_backed_cases`, and per-case transcript paths.
- `ci_artifacts/demo/windows/summary.json`: Windows bridge discovery summary.
- `ci_artifacts/demo/windows-gpt/summary.json`: Windows GPT tool-call proof matrix when generated by this hardening campaign.
- `ci_artifacts/demo/repo-ingestion/summary.json`: repo ingestion GPT proof when generated by this hardening campaign.
- `docs/sponsor/caveats.md`: proof boundary and caveats.

These are GitHub Actions proof artifacts. App-user downloads remain backend blob
downloads served through `/api/download/{job_id}/{filename}`.

## Fast Proof Workflows

- [Sponsor Demo Report Only](https://github.com/evanking12/mcp-factory/actions/workflows/sponsor-demo-report-only.yml) re-renders report artifacts from an existing run.
- [Sponsor Repo Ingestion E2E](https://github.com/evanking12/mcp-factory/actions/workflows/sponsor-repo-ingestion-e2e.yml) proves repo/folder ingestion without starting the Windows VM.
- [Sponsor Windows GPT Matrix](https://github.com/evanking12/mcp-factory/actions/workflows/sponsor-windows-gpt-matrix.yml) reuses Windows discovery artifacts and runs only the Windows GPT proof matrix.

## Interpretation

Use `sponsor-report.html` for live review and `final-summary.json` for exact
automation status. The caveat page should be shown when discussing CORBA, RPC,
JNDI, DCOM, or arbitrary closed-source binary recovery. JSON-RPC, SOAP, and SQL
are runtime-backed in the expanded proof; CORBA remains adapter-backed unless a
future campaign adds a verified ORB/IIOP runtime.
