# Lab Notes

## 2026-01-19
**Goal:** Initialize repo skeleton + commit working dumpbin→CSV prototype artifacts.  
**Work done:**
- Created repo structure (docs/src/scripts/tests/artifacts)
- Added initial templates (meeting notes, weekly update)
- Added MVP script + example output (libzstd)

**Commands run (local):**
- (add exact commands here)

**Outputs produced:**
- artifacts/libzstd/libzstd_api_improved.csv
- (optional) artifacts/libzstd/libzstd_exports_raw.txt

**Next steps:**
- Add demangling support (`undname`) and confidence/provenance columns
- Add one fixture-based parser test
