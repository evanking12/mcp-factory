# Active Prompt

Fix sponsor pushbacks in strict order:

1. Document caveats and the static proof index.
2. Promote JSON-RPC to a stricter JSON-RPC 2.0 hosted runtime.
3. Add repo ingestion proof from a deterministic fixture.
4. Add Windows GPT proof from existing bridge summaries using
   `tool_result_observed` semantics.
5. Wire fast workflows and final report sections.
6. Commit, push, and run focused workflows before full Sponsor Demo E2E.

Do not weaken the existing green sponsor gate. New evidence can be optional in
old report-only artifacts but must be observable when the hardening workflows
run.
