# 006 UI Proof Matrix

Status: implemented.

Gate:
- UI proof link points to current canonical proof run.
- UI includes a legacy protocol proof matrix.
- UI guided showcase loads a SOAP/WSDL demo target.
- Chat still renders `tool_call` and `tool_result` events and downloads via
  `/api/download/{job_id}/{filename}`.

