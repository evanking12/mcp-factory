# Review Notes: Campaign Start

- The campaign excludes Ghidra and binary recovery, matching the user's current
  requested path.
- The focused workflow must not pass with `--client-mode local`.
- The temporary Azure client VM must be cleaned up unless explicitly retained
  for debugging.
- Report promotion waits on artifact evidence; docs must not claim remote DCOM
  before the focused proof passes.
